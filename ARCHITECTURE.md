# 🏗️ Архитектура проекта

## 📁 Структура проекта

```
TelegrambotWoo/
├── bot.py                 # Основной файл с логикой бота
├── config.py              # Конфигурация и настройки
├── image_utils.py         # Утилиты для работы с изображениями
├── check_api.py           # Скрипт проверки API подключений
├── requirements.txt       # Зависимости Python
├── .env.example          # Пример файла конфигурации
├── .gitignore            # Игнорируемые файлы для Git
├── install.sh            # Скрипт автоматической установки
├── README.md             # Основная документация
├── QUICKSTART.md         # Руководство по быстрому старту
└── EXAMPLES.md           # Примеры использования
```

## 🔄 Поток работы (Workflow)

```
┌─────────────┐
│   Начало    │
│   /start    │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ Загрузка фотографий │
│    (3-4 штуки)      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   AI Анализ фото    │
│   (GPT-4 Vision)    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Получение данных   │
│  Материал → Размер  │
│      → Цена         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Генерация фото     │
│  модели (DALL-E 3)  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Проверка & Редакт.  │
│  (Интерактивные     │
│     кнопки)         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Загрузка товара    │
│  в WooCommerce      │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Готово!   │
└─────────────┘
```

## 🧩 Основные компоненты

### 1. bot.py - Основной модуль

**Классы:**

```python
class ProductData:
    """Хранит все данные о продукте"""
    - photos: List[bytes]           # Исходные фотографии
    - photo_ids: List[str]          # ID фото в Telegram
    - name: str                     # Название товара
    - description: str              # Описание товара
    - color: str                    # Цвет товара
    - category: str                 # Категория товара
    - material: str                 # Материал изделия
    - size: str                     # Размер изделия
    - price: str                    # Цена в рублях
    - generated_image: bytes        # Сгенерированное фото
    - use_generated_image: bool     # Флаг использования
```

**Основные функции:**

- `start()` - Инициализация нового товара
- `receive_photo()` - Прием фотографий от пользователя
- `photos_done()` - Завершение загрузки фото и запуск анализа
- `analyze_photos()` - Анализ фото через GPT-4 Vision
- `generate_model_photo()` - Генерация фото модели через DALL-E 3
- `receive_material/size/price()` - Получение доп. данных
- `button_handler()` - Обработка нажатий кнопок
- `upload_to_woocommerce()` - Загрузка товара в магазин

**Состояния ConversationHandler:**

```python
WAITING_PHOTOS       # Ожидание фотографий
WAITING_MATERIAL     # Ожидание материала
WAITING_SIZE         # Ожидание размера
WAITING_PRICE        # Ожидание цены
WAITING_CONFIRMATION # Ожидание подтверждения/редактирования
```

### 2. config.py - Конфигурация

**Содержимое:**

- `PRODUCT_CATEGORIES` - Категории товаров и промпты для генерации
- `OPENAI_CONFIG` - Настройки OpenAI API
- `ANALYSIS_PROMPT` - Промпт для анализа фото
- `MODEL_PHOTO_PROMPT_TEMPLATE` - Шаблон для генерации фото
- `WOOCOMMERCE_CONFIG` - Настройки WooCommerce
- `TELEGRAM_CONFIG` - Настройки Telegram бота
- `LIMITS` - Лимиты и ограничения
- `LOGGING_CONFIG` - Настройки логирования

**Функции:**

- `get_category_prompt()` - Получение промпта по категории
- `validate_price()` - Валидация цены
- `validate_text_length()` - Валидация длины текста

### 3. image_utils.py - Работа с изображениями

**Функции:**

- `resize_image()` - Изменение размера изображения
- `optimize_image_for_telegram()` - Оптимизация для Telegram
- `compress_image()` - Сжатие изображения до целевого размера
- `get_image_info()` - Получение информации об изображении
- `create_thumbnail()` - Создание миниатюры

### 4. check_api.py - Проверка API

**Функции:**

- `check_telegram()` - Проверка Telegram Bot Token
- `check_openai()` - Проверка OpenAI API Key
- `check_woocommerce()` - Проверка WooCommerce API

## 🔌 Интеграции

### OpenAI API

**Используемые модели:**

1. **GPT-4 Vision (gpt-4o)**
   - Назначение: Анализ фотографий товара
   - Вход: 1-2 фотографии в base64
   - Выход: JSON с названием, описанием, цветом, категорией
   - Стоимость: ~$0.03-0.05 за запрос

2. **DALL-E 3**
   - Назначение: Генерация фото модели с товаром
   - Вход: Текстовый промпт
   - Выход: Изображение 1024x1024
   - Стоимость: ~$0.04 (standard) / $0.08 (HD)

**Пример запроса к GPT-4 Vision:**

```python
response = openai_client.chat.completions.create(
    model="gpt-4o",
    messages=[
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "Промпт анализа"},
                {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,..."}}
            ]
        }
    ],
    max_tokens=500
)
```

**Пример запроса к DALL-E 3:**

```python
response = openai_client.images.generate(
    model="dall-e-3",
    prompt="Professional product photography: Beautiful young woman...",
    size="1024x1024",
    quality="standard",
    response_format="b64_json"
)
```

### WooCommerce REST API

**Используемые эндпоинты:**

1. **GET /wp-json/wc/v3/system_status**
   - Проверка подключения

2. **POST /wp-json/wp/v2/media**
   - Загрузка изображений в медиатеку

3. **POST /wp-json/wc/v3/products**
   - Создание товара

4. **GET /wp-json/wc/v3/products**
   - Получение списка товаров

**Пример создания товара:**

```python
product_data = {
    "name": "Вязаная шапка",
    "type": "simple",
    "regular_price": "1500",
    "description": "Описание товара",
    "images": [
        {"id": 123, "src": "https://..."}
    ],
    "categories": [
        {"name": "Шапки"}
    ],
    "attributes": [
        {
            "name": "Цвет",
            "options": ["Синий"],
            "visible": True
        }
    ]
}

response = wcapi.post("products", product_data)
```

### Telegram Bot API

**Используемые обработчики:**

- `CommandHandler` - Обработка команд (/start, /done, /cancel)
- `MessageHandler` - Обработка сообщений (фото, текст)
- `CallbackQueryHandler` - Обработка нажатий на кнопки
- `ConversationHandler` - Управление состояниями диалога

**Пример кнопок:**

```python
keyboard = [
    [InlineKeyboardButton("✅ Загрузить", callback_data="upload")],
    [InlineKeyboardButton("🔄 Изменить фото", callback_data="regenerate")],
    [InlineKeyboardButton("📝 Изменить описание", callback_data="edit_desc")]
]
reply_markup = InlineKeyboardMarkup(keyboard)
```

## 🔐 Безопасность

### Хранение секретов

- Все API ключи хранятся в `.env` файле
- `.env` добавлен в `.gitignore`
- Использование `python-dotenv` для загрузки переменных

### Валидация данных

- Проверка формата цены
- Ограничение длины текста
- Валидация размера файлов
- Проверка типов файлов

### Обработка ошибок

```python
try:
    # Операция
    result = api_call()
except APIError as e:
    # Логирование и уведомление пользователя
    logger.error(f"API Error: {e}")
    await update.message.reply_text("Ошибка API")
```

## 📊 Мониторинг и логирование

### Логи бота

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()
    ]
)
```

### Метрики

Для production рекомендуется добавить:

- Количество обработанных товаров
- Время обработки запросов
- Количество ошибок API
- Стоимость использования OpenAI

## 🚀 Масштабирование

### Для высоких нагрузок

1. **Очередь задач** - Использовать Celery + Redis
2. **Кэширование** - Redis для кэша результатов анализа
3. **База данных** - PostgreSQL для хранения истории
4. **Мониторинг** - Prometheus + Grafana

### Оптимизация затрат

1. Кэширование результатов анализа похожих товаров
2. Пакетная обработка изображений
3. Использование более дешевых моделей где возможно
4. Сжатие изображений перед отправкой в API

## 🔧 Расширение функционала

### Возможные улучшения

```python
# 1. Поддержка вариативных товаров
def create_variable_product(product_data, variations):
    """Создание товара с вариантами размера/цвета"""
    pass

# 2. Автоматическое определение цены
def suggest_price(category, material, similar_products):
    """AI-определение оптимальной цены"""
    pass

# 3. Мультиязычность
def translate_product(product_data, target_language):
    """Перевод описания на другие языки"""
    pass

# 4. SEO-оптимизация
def generate_seo_metadata(product_data):
    """Генерация meta-тегов и keywords"""
    pass

# 5. Интеграция с Instagram
def import_from_instagram(post_url):
    """Импорт товара из поста Instagram"""
    pass
```

## 📚 Используемые библиотеки

### Основные

- **python-telegram-bot** (21.0) - Telegram Bot API
- **openai** (1.54.0) - OpenAI API
- **woocommerce** (3.0.0) - WooCommerce REST API
- **Pillow** (10.4.0) - Обработка изображений
- **requests** (2.31.0) - HTTP запросы
- **python-dotenv** (1.0.0) - Загрузка .env

### Зависимости

```
python-telegram-bot → urllib3, certifi
openai → httpx, pydantic
woocommerce → requests
Pillow → (native C libraries)
```

## 🧪 Тестирование

### Рекомендуемые тесты

```python
# tests/test_bot.py
def test_analyze_photos():
    """Тест анализа фотографий"""
    pass

def test_generate_model_photo():
    """Тест генерации фото модели"""
    pass

def test_upload_to_woocommerce():
    """Тест загрузки в WooCommerce"""
    pass

# tests/test_config.py
def test_validate_price():
    """Тест валидации цены"""
    pass

# tests/test_image_utils.py
def test_resize_image():
    """Тест изменения размера"""
    pass
```

## 📝 Версионирование

Текущая версия: **1.0.0**

Семантическое версионирование: `MAJOR.MINOR.PATCH`

- MAJOR: Несовместимые изменения API
- MINOR: Новая функциональность (обратно совместимая)
- PATCH: Исправления ошибок

## 🤝 Вклад в проект

При добавлении нового функционала:

1. Создайте отдельную ветку
2. Добавьте тесты
3. Обновите документацию
4. Создайте Pull Request

## 📞 Поддержка

Для вопросов по архитектуре:
- Создайте Issue с меткой `question`
- Опишите желаемый функционал
- Предложите свою реализацию

---

**Документ обновлен:** 2024-12-09
**Версия:** 1.0.0
