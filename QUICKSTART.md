# 🚀 Быстрый старт

> ⚠️ Обновление: вместо OpenAI/DALL·E используется связка Anthropic Claude для анализа и Replicate FLUX для генерации/инпейтинга. См. README.md для свежих инструкций.

## Шаг 1: Установка зависимостей

```bash
# Перейдите в директорию проекта
cd ClaudeProjects/TelegrambotWoo

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 2: Получение API ключей

### Telegram Bot

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте `/newbot`
3. Придумайте имя для бота
4. Скопируйте токен

### Anthropic Claude (анализ изображений)

1. Получите ключ на [console.anthropic.com](https://console.anthropic.com/)
2. Сохраните его как `ANTHROPIC_API_KEY`

### Replicate (FLUX)

1. Создайте токен на [replicate.com/account/api-tokens](https://replicate.com/account/api-tokens)
2. Сохраните его как `REPLICATE_API_TOKEN`
3. По умолчанию используется `black-forest-labs/flux-schnell` (максимум **4 шага**). Если хотите другую модель в preview, задайте `FLUX_BASE_MODEL_PREVIEW`.

### WooCommerce

1. Войдите в админ-панель WordPress
2. **WooCommerce → Настройки → Дополнительно → REST API**
3. Нажмите **Добавить ключ**
4. Права доступа: **Чтение/Запись**
5. Сохраните Consumer Key и Consumer Secret

## Шаг 3: Настройка .env

```bash
# Скопируйте пример
cp .env.example .env

# Откройте .env в текстовом редакторе и заполните:
nano .env  # или любой другой редактор
```

Пример заполнения:
```env
TELEGRAM_BOT_TOKEN=1234567890:AAAAAAA-ExampleToken-BBBBBBBBBBB
ANTHROPIC_API_KEY=sk-ant-api-key
REPLICATE_API_TOKEN=r8_...
FLUX_BASE_MODEL=black-forest-labs/flux-schnell
FLUX_BASE_MODEL_PREVIEW=black-forest-labs/flux-schnell
FLUX_FILL_MODEL=black-forest-labs/flux-fill-pro
MAX_SIZE=512
STEPS_PREVIEW=4
STEPS_HQ=4
```

> `flux-schnell` поддерживает максимум 4 шага. Значения `STEPS_PREVIEW`/`STEPS_HQ` выше 4 будут автоматически уменьшены. Для HQ можно выбрать более тяжёлую модель (например, `flux-pro`) через `FLUX_BASE_MODEL`, а для preview — отдельную через `FLUX_BASE_MODEL_PREVIEW`.

## Шаг 4: Проверка подключения

```bash
# Запустите тест подключения
python check_api.py
```

Вы должны увидеть:
```
✅ Telegram Bot: Подключен
✅ OpenAI: Подключен
✅ WooCommerce: Подключен
```

## Шаг 5: Запуск бота

```bash
python bot.py
```

При успешном запуске:
```
🤖 Бот запущен!
```

## Шаг 6: Использование

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Загрузите 3-4 фото товара
4. Отправьте `/done`
5. Следуйте инструкциям бота

## 💡 Советы

### Качество фотографий

- Используйте хорошее освещение
- Фотографируйте на нейтральном фоне
- Показывайте товар с разных ракурсов
- Избегайте размытых фото

### Экономия вызовов Anthropic/Replicate

- Держите `STEPS_PREVIEW`/`STEPS_HQ` на минимально допустимых значениях (для `flux-schnell` — не выше 4)
- Используйте preview-модель через `FLUX_BASE_MODEL_PREVIEW`, если HQ качество не требуется
- Снижайте `MAX_SIZE`, чтобы ускорить генерацию и уменьшить стоимость

### Безопасность

- ❌ Никогда не публикуйте файл `.env`
- ❌ Не делитесь API ключами
- ✅ Используйте `.gitignore`
- ✅ Регулярно обновляйте ключи

## 🆘 Частые проблемы

### Бот не отвечает

```bash
# Проверьте токен
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

### Ошибка OpenAI "Insufficient funds"

Пополните баланс на [platform.openai.com](https://platform.openai.com/account/billing)

### Ошибка WooCommerce 401

Проверьте правильность `WC_KEY` и `WC_SECRET`

### Бот зависает

Проверьте логи:
```bash
tail -f bot.log
```

## 📊 Стоимость

Примерная стоимость за один товар:
- GPT-4 Vision: ~$0.03-0.05
- DALL-E 3: ~$0.04-0.08
- **Итого: ~$0.07-0.13**

## 🔄 Обновление

```bash
# Остановите бота (Ctrl+C)
# Получите последнюю версию
git pull

# Обновите зависимости
pip install -r requirements.txt --upgrade

# Перезапустите
python bot.py
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте `check_api.py`
2. Изучите `README.md`
3. Посмотрите логи `bot.log`
4. Создайте Issue в репозитории

---

**Готово! Теперь вы можете автоматически добавлять товары в WooCommerce! 🎉**
