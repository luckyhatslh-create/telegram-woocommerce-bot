# Исправление ошибки загрузки в WooCommerce

## Проблема

При попытке загрузить товар в WooCommerce возникала ошибка:
```
woocommerce_product_image_upload_error: Ошибка дистанционного получения изображения
```

## Причина

На вашем сервере WooCommerce не может загружать изображения по внешним URL (как base64 data URI, так и обычные HTTP URL). Это связано с ограничениями:
- Отключены внешние HTTP-запросы в WordPress
- Ограничения хостинга/файрвола
- Требуется прямая загрузка через WordPress Media Library API

## Решение

Реализована загрузка изображений через **WordPress Media Library API** с использованием Application Password.

### Что было сделано:

1. ✅ Создан модуль `media_uploader.py` для загрузки изображений
2. ✅ Обновлён `bot.py` для использования нового метода загрузки
3. ✅ Добавлена инструкция `SETUP_MEDIA_UPLOAD.md`
4. ✅ Создан тестовый скрипт `test_media_upload.py`
5. ✅ Обновлён `.env.example` с новыми переменными

### Как настроить:

#### Шаг 1: Создайте Application Password

1. Зайдите в админку WordPress: https://luckyhats.ru/wp-admin
2. Перейдите: **Пользователи** → **Профиль**
3. Найдите секцию **Application Passwords**
4. Создайте новый пароль с именем "TelegramBot"
5. Скопируйте пароль (показывается один раз!)

#### Шаг 2: Добавьте данные в .env

Отредактируйте файл `.env` и добавьте:

```bash
# WordPress Authentication
WP_USERNAME=admin
WP_APP_PASSWORD=xxxx_xxxx_xxxx_xxxx_xxxx_xxxx
```

Замените:
- `admin` - на ваш логин WordPress
- `xxxx_xxxx_xxxx_xxxx_xxxx_xxxx` - на пароль без пробелов

#### Шаг 3: Протестируйте

```bash
python3 test_media_upload.py
```

Если видите "✅ УСПЕХ!", значит всё настроено правильно!

#### Шаг 4: Запустите бота

```bash
python3 bot.py
```

Теперь бот будет загружать изображения через Media Library API.

## Альтернативные решения

Если не хотите настраивать Application Password:

### Вариант 1: Включить HTTP-запросы в WordPress

Добавьте в `wp-config.php`:
```php
define('WP_HTTP_BLOCK_EXTERNAL', false);
```

### Вариант 2: Работать без изображений

Бот автоматически создаст товар без изображений, если не настроены учётные данные WordPress. Изображения можно будет добавить вручную в админке.

### Вариант 3: Использовать внешний хостинг

Настройте загрузку изображений на Cloudinary, ImgBB и используйте их URL.

## Проверка работы

После настройки Application Password:

```bash
# Тест загрузки изображения
python3 test_media_upload.py

# Тест создания товара
python3 test_woocommerce_product.py

# Запуск бота
python3 bot.py
```

## Важные замечания

⚠️ **Telegram URL изображений действительны только 1 час!**
- Бот должен загрузить товар в течение часа после получения фото
- Если прошло больше часа, фото из Telegram станут недоступными

✅ **Сгенерированные OpenAI изображения** работают дольше
- URL от DALL-E действительны несколько часов
- Рекомендуется использовать сгенерированные изображения

## Логи

При загрузке товара бот выводит детальные логи:
```
📤 Используется загрузка через WordPress Media Library
Загрузка изображения 1/3...
✅ Изображение 1 загружено (ID: 123)
✅ Товар успешно создан: ID 456
```

Если что-то не работает, логи помогут найти проблему.
