# 📤 Инструкция по публикации на GitHub

> ⚠️ Памятка устарела: стек OpenAI заменён на Anthropic Claude + Replicate FLUX. Ориентируйтесь на README.md при сборке и запуске.

## ✅ Что уже сделано:

1. ✅ Инициализирован Git репозиторий
2. ✅ Настроен `.gitignore` для защиты секретных данных
3. ✅ Файл `.env` **НЕ** будет загружен на GitHub
4. ✅ Создан `.env.example` с примерами (без реальных ключей)
5. ✅ Создан первый коммит с чистым кодом
6. ✅ Добавлен LICENSE (MIT)
7. ✅ Подготовлен README для GitHub

## 🚀 Шаги для публикации:

### 1. Создайте репозиторий на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Заполните данные:
   - **Repository name:** `telegram-woocommerce-bot` (или своё название)
   - **Description:** `AI-powered Telegram bot for WooCommerce product management`
   - **Visibility:** Public (или Private если хотите)
   - ❌ **НЕ** ставьте галочки на "Add README" и "Add .gitignore" (у нас уже есть)
4. Нажмите **"Create repository"**

### 2. Свяжите локальный репозиторий с GitHub

Скопируйте URL вашего репозитория (например: `https://github.com/username/telegram-woocommerce-bot.git`)

Выполните команды:

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/ваш-username/telegram-woocommerce-bot.git

# Отправьте код на GitHub
git push -u origin main
```

### 3. Проверьте результат

Откройте ваш репозиторий на GitHub и убедитесь:
- ✅ Все файлы загружены
- ✅ `.env` **НЕТ** в репозитории
- ✅ `.env.example` присутствует
- ✅ README.md отображается

### 4. (Опционально) Замените README

Если хотите использовать более красивый README для GitHub:

```bash
mv README.md README_OLD.md
mv README_GITHUB.md README.md
git add README.md README_OLD.md
git commit -m "Update README for GitHub"
git push
```

## 🔒 Безопасность - Важно!

### ✅ Защищённые файлы (НЕ попадут на GitHub):

- `.env` - все ваши секретные ключи
- `.claude/` - локальные данные Claude
- `.agent/` - локальные данные агентов
- `venv/` - виртуальное окружение Python
- `*.log` - логи
- `__pycache__/` - кэш Python

### ⚠️ Проверьте перед каждым push:

```bash
# Убедитесь что .env НЕ в списке
git status

# Проверьте что .env игнорируется
git check-ignore .env
# Должно вывести: .env

# Посмотрите что будет загружено
git ls-files
```

### 🚨 Если случайно добавили .env:

```bash
# Удалите из индекса (но НЕ удаляйте файл)
git rm --cached .env

# Закоммитьте изменение
git commit -m "Remove .env from tracking"

# Отправьте на GitHub
git push
```

## 📝 Дальнейшие обновления

После внесения изменений в код:

```bash
# Добавьте изменённые файлы
git add .

# Создайте коммит с описанием
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

## 🎨 Улучшения для GitHub

### Добавьте badges в README:

```markdown
![Python](https://img.shields.io/badge/python-3.8+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Telegram](https://img.shields.io/badge/telegram-bot-blue.svg)
```

### Создайте GitHub Topics:

На странице репозитория нажмите "Add topics" и добавьте:
- `telegram-bot`
- `woocommerce`
- `openai`
- `gpt-4`
- `dall-e`
- `python`
- `ai`
- `e-commerce`

### Настройте GitHub Actions (опционально):

Создайте `.github/workflows/test.yml` для автоматического тестирования при каждом push.

## ✅ Чек-лист перед публикацией:

- [ ] `.env` не включён в репозиторий
- [ ] `.env.example` содержит примеры без реальных ключей
- [ ] README.md содержит инструкции по установке
- [ ] Указана лицензия (LICENSE файл)
- [ ] Удалены ненужные тестовые файлы
- [ ] Код прокомментирован
- [ ] requirements.txt актуален

## 🎉 Готово!

После публикации ваш бот будет доступен по адресу:
`https://github.com/ваш-username/telegram-woocommerce-bot`

Другие разработчики смогут:
- Клонировать репозиторий
- Установить зависимости
- Настроить свои ключи в `.env`
- Запустить своего бота

---

**Важно:** После публикации на GitHub **НЕ** делайте коммиты с `.env` файлом!
